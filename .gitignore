.superpowers/
coverage.out
.playwright-cli/
artifacts/
control-plane/node_modules/
control-plane/.next/
control-plane/.next-export/
control-plane/tsconfig.tsbuildinfo
control-plane/tsconfig.build.tsbuildinfo

# build artifacts
bin/
/gateway
/2papi
dist/
*.exe

# local data / env
.env
.env.local
compose.override.yaml

# os junk
Thumbs.db
.DS_Store
%APPDATA%/

# local scratch (never push)
.enola/
scratch/
# Any benchmark config holds a live upstream key. Ignore the whole family and
# keep only the placeholder example tracked, so a second provider config
# cannot slip past a single-filename rule.
test/config-benchmark*.yaml
!test/config-benchmark.example.yaml

# benchmark harness scratch
node_modules/
test/results/matrix_raw.txt
test/results/squoze_artifacts/

# Alternate module file used by the squoze A/B repro scripts
/go.local.mod
/go.local.sum
